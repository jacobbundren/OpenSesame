const initialState = {
  userGuess: ''
};

const userGuessReducer = (state = initialState, action) => {
  switch (action.type) {
    case 'KEYPAD_BUTTON_PRESS': {
      return {
        ...state,

      }
    }
  }
}
